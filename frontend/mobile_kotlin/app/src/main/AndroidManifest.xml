<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permission for Gemini API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jobify">

        <!-- Main Activity as Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Activities -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".RecruiterProfileActivity"
            android:exported="false" />

        <!-- Other App Activities -->
        <activity android:name=".JobOpportunitiesActivity" />
        <activity android:name=".PostsActivity" />
        <activity android:name=".AICareerAdvisorActivity" />
        <activity android:name=".CvCorrectionActivity" />

    </application>

</manifest>
