# Keycloak and database configuration for auth-service2
# Copy this file to `.env` and replace placeholders before starting the service.

KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=jobify-realm
KEYCLOAK_CLIENT_ID=backend
KEYCLOAK_CLIENT_SECRET=Q7Gq8dqHmpZsqcTM4V1HmsQONTLgEs64
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# MongoDB connection used by auth-service2
MONGODB_URI=mongodb://localhost:27017/nest

# Optional: port the NestJS app should listen on (default in code if omitted)
PORT=3000
